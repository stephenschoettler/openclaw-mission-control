Read BRIEF.md in this directory, then build the entire Mission Control NextJS app exactly as specified. Use --dangerously-skip-permissions to auto-approve all file operations. 

Steps:
1. Run: npx create-next-app@14 . --typescript --tailwind --app --src-dir --no-eslint --import-alias "@/*" (answer yes to all prompts)
2. Install deps: npm install better-sqlite3 react-markdown @hello-pangea/dnd lucide-react && npm install --save-dev @types/better-sqlite3
3. Build all 6 panels and API routes as specified in BRIEF.md
4. Set up SQLite at /home/w0lf/.mission-control/db.sqlite
5. Create the systemd service file
6. Run: npm run build
7. When done, run: /home/w0lf/.npm-packages/bin/openclaw system event --text "Done: Mission Control built at ~/mission-control on port 3001. Ready to start." --mode now
